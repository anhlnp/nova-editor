// Empty module stub — used by webpack aliases to silence WS packages whose source
// files use build-time-only syntax (JSX namespaces, ws:style) that SWC rejects.
// Template/meta imports are redirected here; runtime canvas rendering works without them.
module.exports = {};
